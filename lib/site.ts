// Use the apex (non-www) host — that is what resolves in DNS and is served.
// Every canonical tag, sitemap URL, robots.txt entry, Open Graph url, and
// JSON-LD derives from this, so it MUST match the host Google can reach.
export const SITE_URL = "https://golandlordtools.com";
export const SITE_NAME = "Landlord Tools";
export const SITE_TAGLINE = "Security deposit calculators & documents for independent landlords";

// Contact address shown on legal/contact pages. Update to a real inbox.
export const CONTACT_EMAIL = "hello@golandlordtools.com";

// Google AdSense publisher ID, e.g. "ca-pub-1234567890123456".
// Leave empty until AdSense approves the site — ads and the ad script
// only load once this is set AND the visitor has accepted cookies.
export const ADSENSE_CLIENT = "ca-pub-6079116585044991";
